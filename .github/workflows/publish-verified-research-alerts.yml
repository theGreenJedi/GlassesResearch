name: Publish Verified Research Alerts

on:
  pull_request:
    branches: [main]
    paths:
      - 'data/verified-publications.json'
      - 'docs/RESEARCH_NEWS.md'
      - 'scripts/verified_publications.py'
      - 'scripts/stage_verified_publications.py'
      - '.github/workflows/publish-verified-research-alerts.yml'
      - 'cloudflare/verified-research-alerts/**'
      - 'cloudflare/verified-research-canary/**'
  push:
    branches: [main]
    paths:
      - 'data/verified-publications.json'
      - 'docs/RESEARCH_NEWS.md'
      - 'scripts/verified_publications.py'
      - 'scripts/stage_verified_publications.py'
      - '.github/workflows/publish-verified-research-alerts.yml'
      - 'cloudflare/verified-research-alerts/**'
      - 'cloudflare/verified-research-canary/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verified-research-alert-publisher
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Validate verified publication ledger and publisher syntax
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile scripts/verified_publications.py scripts/stage_verified_publications.py
          python scripts/verified_publications.py

  publish:
    if: github.event_name != 'pull_request'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - name: Require publisher credential
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${PUBLISH_TOKEN:-}" ]; then
            echo 'Missing required GitHub Actions secret: PUBLISH_TOKEN' >&2
            exit 1
          fi
      - name: Stage verified research for real subscribers
        run: python scripts/stage_verified_publications.py
      - name: Prove recipient-side delivery with synthetic canary
        run: python scripts/verified_publications.py --publish
      - name: Record end-to-end alert contract
        shell: bash
        run: |
          {
            echo '### Verified Research Alerts end-to-end proof'
            echo ''
            echo '- Public research ledger validated.'
            echo '- Watching and discovery intake are excluded by validation.'
            echo '- Dispatch-enabled entries were staged at the authenticated Worker before canary proof, so real subscriber delivery is not blocked by canary availability.'
            echo '- The run passes end to end only after the synthetic canary recipient records each exact publication ID after SMTP delivery.'
            echo '- Re-running is safe because publication ingestion, subscriber deliveries, and canary receipts are idempotent by publication ID.'
          } >> "$GITHUB_STEP_SUMMARY"

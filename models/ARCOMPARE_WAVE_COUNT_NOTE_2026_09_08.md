# Count impact

Expected active canonical population after mechanical synchronization: **236**. Stable IDs extend through GLS-0237 because the ID sequence contains a historical retired/gap identity and is not itself the active row count.